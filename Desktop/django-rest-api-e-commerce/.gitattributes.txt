# Set text files to use LF in the repository and convert to CRLF on checkout (Windows)
* text=auto

# Force specific line endings for certain file types
*.py text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
